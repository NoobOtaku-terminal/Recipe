{
  "name": "recipe-migrations",
  "version": "1.0.0",
  "description": "Database migration runner for Recipe Battle Platform",
  "main": "run-migrations.js",
  "scripts": {
    "migrate": "node run-migrations.js"
  },
  "dependencies": {
    "pg": "^8.11.3"
  }
}
